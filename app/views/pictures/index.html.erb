<%= render 'layouts/tagbar' %>

<!-- <div class="container my-4">

  <%= form_for pictures_path, method: 'get' do |f| %>
    <div class="field">
      <% @tags.each do |tag| %>
        <%= f.label "#{tag.title}" %>
        <%= f.check_box :search_id, {}, tag.id, '' %>
      <% end %>
    </div>
    <div class="actions">
      <%= f.submit "Trier" %>
    </div>
  <% end %>
  <ul>
  <% @tags.each do |tag| %>
      <li>
        <%= check_box_tag 'search_ids[]', tag.id -%>
        <%= h tag.title -%>
      </li>
  <% end %>
  </ul>

</div> -->

<!-- <div class="picture-index-content">
<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pictures.each do |picture| %>
      <div class="picture-elements">
        <a href="<%= picture_url(picture) %>"><img src="<%= picture.image_url %>", class = "img-home", style="width: 100%; height: 100%; object-fit: cover;"/>
        </a>
      </div>
    <% end %>
  </tbody>
</table> -->

<%= render 'cards' %>

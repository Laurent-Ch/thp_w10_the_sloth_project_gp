<%# Quantité
Nom de produit
Prix unitaire
Prix selon quantité %>

<% if false %>

<% @picture_counter_hash.each do |key, value|%>
    <% @current_pics = Picture.where(id: key) %>
    <div>Quantité: <%= value %></div>
    <% @current_pics.each do |pic| %>
        <img src="<%= pic.image_url%>">
        <%= pic.price %>
        <%= pic.title%>
    <% end %>
<% end %>

<% end %>



<div class="container">
    <h1>Mon panier</h1>
    <br/>
    <br/>
        <div class="row">
            <div class="col-lg-8 col-md-8">
                <table class="table">
                    <thead>
                        <tr>
                        <th scope="col">Quantité</th>
                        <th scope="col">Nom de produit</th>
                        <th scope="col">Image</th>
                        <th scope="col">Prix unitaire</th>
                        <th scope="col">Prix selon quantité</th>
                        <th scope="col"></th>
                        </tr>
                    </thead>
                    <tbody>
                        <% @picture_counter_hash.each do |key, value|%>
                        <% @current_pics = Picture.where(id: key) %>
                            <tr>
                            <td> <%= value %> </td>
                            <% @current_pics.each do |pic| %>
                            </th>
                            <td><%= pic.title%></td>
                            <td><img class="thumbnail ml-5 rounded-circle" src="<%= pic.image_url%>"></td>
                            <td> <%= pic.price %>€</td>
                            <td> <%= value * pic.price %>€</td>
                            <td><%= link_to "Supprimer", card_path(key), :class => "btn btn-outline-danger btn-l", :method => 'DELETE', :data => {:confirm => 'Veux-tu vraiment supprimer cet article ?'} %></td>
                            </tr>
                            <% end %>
                            <% end %>

                         <tr>
                            <th scope="row"></th>
                            <td></td>
                            <td></td></td>
                            <td></td>
                            </tr>
                            <tr>
                            <th scope="row"></th>
                            <td></td>
                            <td>Prix total : <%= session[:amount] %>€</td>
                            </tr>
                    </tbody>
                </table>
            </div>
            <%=link_to 'Passer la commande', new_order_path, class: 'btn btn-success'%>
        </div>
</div>
